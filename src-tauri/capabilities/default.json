{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities. The app loads a remote page that does not use Tauri IPC; all control happens Rust-side via eval, so only core defaults are granted.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
